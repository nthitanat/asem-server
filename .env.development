# Development Environment Configuration
NODE_ENV=development
PORT=5001
API_VERSION=v1

# Database Configuration (Local MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=asem_dev_db
DB_USER=thitanat.n
DB_PASSWORD=Koontounza4943
DB_CONNECTION_LIMIT=20

# JWT Configuration (Less secure for development)
JWT_SECRET=dev-secret-key-not-secure-just-for-testing-min-32-chars-long
JWT_ACCESS_EXPIRY=1h
JWT_REFRESH_EXPIRY=7d

# Token Expiry (in seconds)
EMAIL_VERIFICATION_EXPIRY=86400    # 24 hours
PASSWORD_RESET_EXPIRY=3600         # 1 hour

# Email Configuration - Testing (Use Mailtrap or Ethereal)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_SECURE=false
SMTP_USER=your-mailtrap-user
SMTP_PASS=your-mailtrap-password
FROM_EMAIL=dev@asem.local
FROM_NAME=ASEM Dev Server

# Frontend URL (Local)
FRONTEND_URL=http://localhost:3000

# Security Configuration (Relaxed for development)
BCRYPT_ROUNDS=4
RATE_LIMIT_WINDOW_MS=900000        # 15 minutes
RATE_LIMIT_MAX_REQUESTS=1000       # Higher limit for testing

# CORS Configuration (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Email Verification
EMAIL_VERIFICATION_ENABLED=true
